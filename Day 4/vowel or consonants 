#include <stdio.h>

int main() {
    char ch;
    int is_vowel = 0; 

    printf("Enter an alphabet: ");
    scanf(" %c", &ch);

    if (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u' ||
        ch == 'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U') {
        
        is_vowel = 1;
    }

    if (is_vowel == 1) {
        printf("'%c' is a VOWEL.\n", ch);
    } else {
        printf("'%c' is a CONSONANT.\n", ch);
    }

    return 0;
}
