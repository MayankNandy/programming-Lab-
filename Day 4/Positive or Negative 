#include <stdio.h>

int main() {
    int num;

    printf("Enter an integer: ");
    scanf("%d", &num);

    // Ladder Logic (Positive, Negative, Zero)
    if (num > 0) {
        printf("The number is POSITIVE.\n");
    } else if (num < 0) {
        printf("The number is NEGATIVE.\n");
    } else {
        printf("The number is ZERO.\n");
    }

    return 0;
}
